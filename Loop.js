const array = ['a', 'b', 'c'];
for (const element of array) {
  console.log(element);
}
return this.array[0];

// const obj = { x: 1, y: 2 };
// for (const key in obj) {
//   console.log(key, obj[key]);
// }
